<Window x:Class="EShellULoadSetTools.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Processing"
        Height="280"
        Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False">
    <Window.Resources>
        <CubicEase x:Key="EaseInOutCubic" EasingMode="EaseInOut" />
        <Storyboard x:Key="SpinnerAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="SpinnerVisual"
                             Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                             From="0"
                             To="360"
                             Duration="0:0:1.2"
                             EasingFunction="{StaticResource EaseInOutCubic}" />
        </Storyboard>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText"
                   Text="Processing..."
                   FontWeight="SemiBold"
                   FontSize="14"
                   TextWrapping="Wrap" />

        <Grid x:Name="SpinnerContainer"
              Grid.Row="1"
              Margin="0,16,0,0"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">
            <Viewbox Width="200" Height="200">
                <Grid x:Name="SpinnerVisual"
                      Width="200"
                      Height="200"
                      RenderTransformOrigin="0.5,0.5">
                    <Grid.RenderTransform>
                        <RotateTransform />
                    </Grid.RenderTransform>
                    <Grid.Resources>
                        <Style TargetType="Ellipse">
                            <Setter Property="Width" Value="200" />
                            <Setter Property="Height" Value="200" />
                            <Setter Property="StrokeThickness" Value="20" />
                        </Style>
                    </Grid.Resources>

                    <Ellipse Stroke="#FFE6E6E6" />
                    <Path Stroke="#FF0A84FF"
                          StrokeThickness="20"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          Data="M100,20 A80,80 0 1 1 99.99,20" />
                </Grid>
            </Viewbox>
        </Grid>
    </Grid>
</Window>
